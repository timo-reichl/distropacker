[Arch]
base_name = arch
build_command = makepkg -s
docker_base = archlinux
docker_base_tag = latest

[Manjaro]
base = Arch

[Debian]
base_name = debian
build_command = dpkg-buildpackage -us -uc
docker_base = debian
docker_base_tag = latest

[Ubuntu]
base = Debian
